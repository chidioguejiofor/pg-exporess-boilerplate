require("dotenv/config");
const path = require("path");

module.exports = {
  config: path.resolve("infrastructure", "db", "config.js"),
  "migrations-path": path.resolve("infrastructure", "db", "migrations"),
};
